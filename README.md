# LRU-cache-assignment-typescript

A least recently used cache example in typescript

To run ```yarn && node chisel.js```

To recompile .ts file run ```yarn run tsc chisel.ts```
And to run again ```node chisel.js```

Cheers,

Nisten   
